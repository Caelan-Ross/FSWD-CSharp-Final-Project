@model IEnumerable<MACK.Models.Drivetrain>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DrivetrainType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VehicleId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DrivetrainType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VehicleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DrivetrainId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DrivetrainId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DrivetrainId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
